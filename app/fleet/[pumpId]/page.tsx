// Layout-driven: `app/fleet/layout.tsx` mounts `FleetShell`, which reads
// `pumpId` via `useParams()` and renders the master-detail view. The page
// itself renders nothing.
export default function FleetPumpPage() {
  return null;
}
